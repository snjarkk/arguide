# 計画: ARドット表示ナビゲーションWebアプリの実装 (平面直角座標対応)

## 目的
日本の平面直角座標（X, Y）を入力すると、iPhoneのカメラ映像上にドットを表示し、視覚的に目的地まで案内するWebアプリを作成する。

## 主要機能
1. **カメラ背景表示**: 背面カメラのリアルタイム映像を全画面で表示。
2. **座標入力 (日本の平面直角座標系)**:
    - **系番号選択**: 日本の1系〜19系をドロップダウンで選択。
    - **X軸・Y軸入力**: キーボードによる数値入力（公共測量座標）。
3. **座標変換**: 入力された(X, Y, 系)を、内部的に緯度・経度に変換するロジックを搭載。
4. **AR表示**: 現在地とデバイスの向きに基づき、目的地の方角にドットを表示。
5. **距離表示**: 目的地までの直線距離を「目標まで約12.34m」のように、小数点第2位までリアルタイムで明確に表示。
6. **iOSセンサー対応**: iOS Safariで方位磁石（ジャイロ）と位置情報を取得するための権限リクエスト。

## 技術スタック
- HTML5 / CSS3 / JavaScript (Vanilla JS)
- Geolocation API (位置情報)
- DeviceOrientation API (方位・傾き)
- MediaDevices API (カメラ)

## 実装ステップ

### 1. UIの構築
- 全画面の `<video>` 要素（カメラ用）。
- 目的地入力フォーム（系番号のselect、X/Yのinput）。
- 案内用のARマーカー（ドット）。
- 距離表示用のテキストラベル（「目標まで約◯◯.◯◯m」）。
- 「案内開始」ボタン（iOSの権限取得とカメラ起動のトリガー）。

### 2. カメラ機能の実装
- `navigator.mediaDevices.getUserMedia` を使用して背面カメラを取得。

### 3. 位置情報と方位の取得
- `navigator.geolocation.watchPosition` で現在地を監視。
- `deviceorientation` イベントで `webkitCompassHeading`（絶対方位）を取得。

### 4. 計算ロジックの実装
- **座標変換**: 平面直角座標(X, Y)から緯度・経度(Lat, Lon)への変換（ガウス・クリューゲル投影の逆計算アルゴリズム）。
- **距離計算**: 現在地から目的地までの距離を計算し、画面上にリアルタイムで表示。
- **方位角（Bearing）**: 現在地から目的地への角度を計算。
- **描画座標**: 画面のFOV（視野角）に基づき、ドットの表示位置を計算。

### 5. 仕上げ
- スマートフォン向けに最適化されたUI/UX。

## 検証項目
- 平面直角座標を入力した際、正しく緯度経度に変換されるか。
- 距離が小数点2位まで正しく更新されるか。
- iPhone実機で方位センサーとカメラが連動するか。
